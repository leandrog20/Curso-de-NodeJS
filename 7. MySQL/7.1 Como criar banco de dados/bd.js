// Primeiro sera necessario acessar o seu banco de dados MySQL, pelo cmd.

// usando o comando: 

// mysql -h localhost -u root -p

// "-h" é o host, "-u" é o usuário e "-p" é a senha.

// Para criar um banco de dados, use o seguinte comando:

create database nome_do_banco;

// Para ver os bancos de dados existentes, use o seguinte comando:

show databases;

// Para usar o banco de dados que você acabou de criar, use o seguinte comando:

use nome_do_banco;